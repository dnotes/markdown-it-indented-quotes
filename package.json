{
  "name": "markdown-it-indented-quotes",
  "version": "3.0.0",
  "description": "A markdown-it plugin that renders indented text blocks as blockquotes instead of code",
  "main": "index.js",
  "scripts": {
    "build": "make build",
    "coverage": "yarn test && nyc report -r text-lcov | coveralls",
    "lint": "eslint .",
    "pub": "yarn lint && yarn test && make build && make publish",
    "test": "nyc --check-coverage --lines 100 --branches 100 mocha"
  },
  "keywords": [
    "markdown-it",
    "markdown"
  ],
  "author": "David Hunt <git@dnotes.net> (https://dnotes.net/)",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.2.3",
    "coveralls": "^3.0.3",
    "eslint": "^5.16.0",
    "markdown-it": "^8",
    "markdown-it-attrs": "^2",
    "markdown-it-testgen": "^0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "terser": "^4.8.1"
  },
  "dependencies": {},
  "repository": "github.com/dnotes/markdown-it-indented-quotes"
}
